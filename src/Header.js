// import React from 'react';

// export default function Header(props) {
// 	return (
// 		<div>
// 			{props.data.map((each, i) => {
// 				return (
// 					<img key={`team-${i}`} onClick={() => props.select(each)} src={`../assets/img/${each}.png`} />
// 				)
// 			})}
// 		</div>	
// 		)
// }